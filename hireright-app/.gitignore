node_modules
.next
.data
*.tsbuildinfo
next-env.d.ts
.env.local

# Real-resume test corpus — fetched on demand via
# `node scripts/fetch-real-resumes.mjs`. Contains real people's PII
# (names, emails, phone numbers); never commit it.
test-data/real-resumes/
